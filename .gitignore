.github
.obsidian
.idea
.snow
cached_layouts
.temp

# Local bash config (gitignored, sourced by .bashrc if present)
**/.config/bash/*.local.sh
# .bash_local.sh only for MSYS2
.bash_local.sh

# Fish universal variables (machine-local runtime state)
**/.config/fish/fish_variables

# Mise local secrets (machine-local, never committed)
**/.config/mise/*.local.toml
