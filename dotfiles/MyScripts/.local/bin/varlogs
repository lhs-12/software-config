#!/usr/bin/env bash
# varlogs - Show all text logs in /var/log with tail -f
# Usage: varlogs

sudo find /var/log -type f -exec file {} \; |
    grep 'text' | cut -d' ' -f1 | sed 's/:$//' |
    grep -v '[0-9]$' |
    xargs tail -f
